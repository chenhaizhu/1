// DO: 创建一个没有任何参数的函数 addFive。 此函数使 sum 变量加 5，但其返回值是 undefined。

// 设置
let sum = 0;

function addThree() {
    sum = sum + 3;
}

// 只修改这一行下面的代码
function addFive() {
    sum += 5;
}
// 只修改这一行上面的代码

addThree();
addFive();
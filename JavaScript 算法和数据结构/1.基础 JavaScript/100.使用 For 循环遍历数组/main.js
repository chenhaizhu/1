// DO: 声明并初始化一个变量 total 值为 0。 使用 for 循环，使得 total 的值为 myArr 的数组中的每个元素的值的总和。

// 设置
const myArr = [2, 3, 4, 5, 6];

// 只修改这一行下面的代码
let total = 0;
for (let i = 0; i < myArr.length; i++) {
    total += myArr[i];
}
// DO: 使用方括号读取 testObj 中 an entree 和 the drink 的属性值，并分别将它们赋值给 entreeValue 和 drinkValue。

// 设置
const testObj = {
    "an entree": "hamburger",
    "my side": "veggies",
    "the drink": "water"
};

// 只修改这一行下面的代码
const entreeValue = testObj["an entree"];   // 修改这一行
const drinkValue = testObj["the drink"];    // 修改这一行
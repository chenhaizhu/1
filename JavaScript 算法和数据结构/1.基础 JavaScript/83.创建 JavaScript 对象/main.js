// DO: 确保对象表示一只名为 myDog 的狗，包含属性 name（字符串）、legs、tails 和 friends。
// 你可以随意设置这些对象的属性值，只要 name 是字符串，legs 和 tails 是数字，friends 是数组即可。

const myDog = {
    // 只修改这一行下面的代码
    "name": "dog",
    "legs": 4,
    "tails": 1,
    "friends": ["cat", "pig"]
    // 只修改这一行上面的代码
};
// 将 myArray 中的第二个元素（索引为 1）设置为除了 b 以外的任意值。

/*
let myArray = ["a", "b", "c", "d"];
// 只修改这一行下面的代码

// 只修改这一行上面的代码
console.log(myArray);
*/

//               ↓

let myArray = ["a", "b", "c", "d"];
// 只修改这一行下面的代码
myArray[1] = "e";
// 只修改这一行上面的代码
console.log(myArray);
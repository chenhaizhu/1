// 我们已经定义了数组 arr。 请使用 splice() 从 arr 里移除元素，使剩余的元素之和为 10。

/*
const arr = [2, 4, 5, 1, 7, 5, 2, 1];
// 只修改这一行下面的代码

// 只修改这一行上面的代码
console.log(arr);
*/

//               ↓

const arr = [2, 4, 5, 1, 7, 5, 2, 1];
// 只修改这一行下面的代码
arr.splice(1, 4);
// 只修改这一行上面的代码
console.log(arr);
// 使用上一个挑战中的 Dog 构造函数创建一个 Dog 的新实例，并把它赋值给变量 hound。
/*
function Dog() {
  this.name = "Rupert";
  this.color = "brown";
  this.numLegs = 4;
}
// 只修改这一行下面的代码
*/

//               ↓

function Dog() {
    this.name = "Rupert";
    this.color = "brown";
    this.numLegs = 4;
}

// 只修改这一行下面的代码
let hound = new Dog();